# نسخ إلى .env للتطوير المحلي (لا ترفع .env إلى Git)
#
# عند التعيين، يطلب الـ API رأس X-Internal-Key مطابقاً لهذه القيمة.
# يجب أن تطابق AI_INTERNAL_KEY في خادم Express الذي يمرر طلبات التحليلات.
# AI_INTERNAL_KEY=
